version: '3.8'

services:
  postgresqlbeavergoose-db:
    image: postgres:14.2
    container_name: postgresqlbeavergoose-db
    environment:
      - POSTGRES_USER=Sina
      - POSTGRES_PASSWORD=sU4m1!rc&1HiNWZZNbB9j!
      - POSTGRES_DB=beavergoose
    ports:
      - "5432:5432"
    volumes:
      - postgresqlbeavergoose-dbdata:/var/lib/postgresql/data
volumes:
  postgresqlbeavergoose-dbdata: